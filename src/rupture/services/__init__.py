"""Application services: use cases composed from adapters and domain models.

A service orchestrates adapters; it is not itself an adapter and implements no port. The only
service in this phase is :mod:`rupture.services.aftershock`.
"""
